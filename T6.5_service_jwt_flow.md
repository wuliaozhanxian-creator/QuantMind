# T6.5 Service JWT 认证流程设计

> 委托方 agent1号 特别指令④：服务间调用改用 service JWT，须出独立的认证流程图供评审。
> 编写人：子 agent-7（安全工程师） | 日期：2026-07-04 | 状态：Phase 1-4 已完成，待项目经理 agent2号 评审
>
> **迁移状态总览（T6.5-P4 更新，2026-07-05）**：
> - Phase 1（基础设施）：已完成 — 死代码清理 + flow doc + `verify_service_token` / `require_service_token` 新增
> - Phase 2（并行运行 + 迁移）：已完成 — 17 个文件迁移至 `X-Service-Token`
> - Phase 3（清理）：已完成 — 33 个文件清理，删除 `get_internal_call_secret()` + 全部 `X-Internal-Call` deprecated 代码
> - Phase 4（文档同步）：已完成 — 所有 README/DOCS/.env.example 同步更新
> - **Residual（纳入 M4 迁移）**：训练容器回调路径 `docker/training/train.py`（发 `X-Internal-Call-Secret`）→ `admin_training_utils.py`（校验 `INTERNAL_CALL_SECRET`）→ `local_docker_orchestrator.py`（注入 `INTERNAL_CALL_SECRET` 环境变量），保留旧共享密钥机制，标注 `T6.5-P3 residual, M4 migration`

---

## 一、背景与现状

### 1.0 迁移后现状（T6.5-P3 完成后）

仓库服务间认证已从 **共享密钥 + X-Internal-Call header** 方案 **完全迁移** 至 **Service JWT + X-Service-Token header** 方案：

- 调用方在请求头附加 `X-Service-Token: <service_jwt>`（由 `create_service_token()` 签发，`SECRET_KEY` 签名）
- 被调用方通过 `backend/shared/auth.py::verify_service_token()` / `require_service_token()` 验签并校验 `service` 声明
- `get_internal_call_secret()` 及 `_DEPRECATED_DEFAULT_SECRET` 已删除（T6.5-P3）
- `X-Internal-Call` header 处理代码已从所有服务间调用路径移除（T6.5-P3）
- **唯一残留**：训练容器回调路径（见 Residual 说明），保留 `INTERNAL_CALL_SECRET` / `X-Internal-Call-Secret`，纳入 M4 迁移

### 1.1 迁移前安全缺陷（X-Internal-Call 旧方案，已修复）

| 缺陷 | 说明 |
|------|------|
| 无调用方身份 | 所有服务共用同一密钥，被调用方无法区分请求来自 api / engine / trade |
| 无时效 | 密钥不过期，泄漏后窗口期长，轮换需所有服务同步重启 |
| 密钥频传 | 每次请求都明文传输共享密钥，日志/中间件泄漏面大 |
| 无审计 | 无法追溯某次内部调用由哪个服务发起 |
| 轮换困难 | 改密钥需全集群协同，运维成本高 |

### 1.2 T6.2 已铺垫的过渡件

`backend/shared/auth.py` 中已存在两个过渡函数（T6.2 引入）：

- `create_service_token(service_name, expire_seconds=300) -> str`
  - 用 `SECRET_KEY`（环境变量）签发短期 JWT，payload 含 `service` / `iat` / `exp`
  - **当前状态：已实现但尚未被任何调用方使用**（grep 确认仅有定义，无调用点）
- `decode_jwt_token(token) -> dict`
  - 用 `SECRET_KEY` 验证签名与过期，返回 payload
  - **当前状态：已被 6 处使用**（request_logging、trade/deps、stream/ws_core、engine/qlib_app/websocket、engine/qlib_app/risk_monitor、api/community/auth）

---

## 二、服务间调用清单（迁移前基于全量 grep get_internal_call_secret，T6.5-P3 后已全部迁移至 X-Service-Token）

### 2.1 调用方 → 被调用方 矩阵（T6.5-P2 已完成迁移）

| 调用方 | 被调用方 | 调用点文件 | 用途 | 迁移状态 |
|--------|----------|-----------|------|---------|
| api | engine | `services/api/routers/engine_proxy.py` | 业务请求代理至 engine | 已迁移 X-Service-Token |
| api | engine | `services/api/routers/ai_ide_proxy.py` | AI IDE 代理至 engine | 已迁移 X-Service-Token |
| api | engine | `services/api/routers/admin/model_management.py` + `model_management_utils.py` | 模型管理代理 | 已迁移 X-Service-Token |
| api | engine | `services/api/routers/admin/strategy_templates.py` | 策略模板代理 | 已迁移 X-Service-Token |
| engine | trade | `services/engine/qlib_app/tasks.py` | pipeline 任务回写 trade | 已迁移 X-Service-Token |
| trade | engine | `services/trade/services/remote_service.py` | 远程服务调用 engine | 已迁移 X-Service-Token |
| trade | engine | `services/trade/services/broker_client.py` | broker 客户端调用 | 已迁移 X-Service-Token |
| trade | engine | `services/trade/simulation/services/execution_engine.py` | 模拟执行引擎调用 | 已迁移 X-Service-Token |
| trade | engine | `services/trade/routers/simulation.py` | 模拟路由调用 | 已迁移 X-Service-Token |
| trade | engine | `services/trade/routers/internal_strategy_utils.py` | 内部策略工具调用 | 已迁移 X-Service-Token |
| trade | (k8s pod) | `services/trade/services/k8s_manager.py` | 向 k8s Pod 注入环境变量 | 已迁移（注入 SECRET_KEY 供 Pod 自行签发 service JWT） |
| trade | (self) | `services/trade/runner/main.py` | runner 自定义 wrapper 发 X-Service-Token | 已迁移 X-Service-Token |
| engine | (self) | `services/engine/routers/ai_ide/config.py` | 引擎内部配置校验 | 已迁移 X-Service-Token |
| engine | (self) | `services/engine/qlib_app/api/admin_templates.py` | 引擎内部模板校验 | 已迁移 X-Service-Token |
| stream | (self/internal) | `services/stream/main.py` | stream 服务内部校验 | 已迁移 X-Service-Token |

### 2.2 被调用方校验点（T6.5-P3 已完成迁移）

- `services/engine/main.py` 中间件：仅对 `/api/v1/internal/*` 路径接受 `X-Service-Token`（service JWT），业务路由必须用户 JWT
- 各 trade / engine 内部路由：通过 `verify_service_token()` / `require_service_token()` 校验 service JWT

### 2.3 训练容器回调路径（T6.5-P3 residual，纳入 M4 迁移）

| 调用方 | 被调用方 | 调用点文件 | 现状 | 残留说明 |
|--------|----------|-----------|------|---------|
| 训练容器 | api | `docker/training/train.py` | 发送 `X-Internal-Call-Secret` header | T6.5-P3 residual, M4 migration |
| api | (self) | `services/api/routers/admin/admin_training_utils.py` | 校验 `INTERNAL_CALL_SECRET` 环境变量 | T6.5-P3 residual, M4 migration |
| engine | 训练容器 | `services/engine/training/local_docker_orchestrator.py` | 向训练容器注入 `INTERNAL_CALL_SECRET` 环境变量 | T6.5-P3 residual, M4 migration |

> **M4 迁移方案**：训练容器 `docker/training/train.py` 改为使用 `SECRET_KEY` 自行签发 service JWT，回调时携带 `X-Service-Token` header；`admin_training_utils.py` 改用 `verify_service_token()` 校验；`local_docker_orchestrator.py` 改为注入 `SECRET_KEY` 而非 `INTERNAL_CALL_SECRET`。

---

## 三、目标 Service JWT 认证流程

### 3.1 总体流程图

```
┌──────────────────────────────────────────────────────────────────────┐
│                        Service JWT 认证流程                          │
└──────────────────────────────────────────────────────────────────────┘

  调用方服务 (api / engine / trade)                  被调用方服务
 ┌───────────────────────────────┐               ┌──────────────────────────┐
 │ 1. 发起服务间调用前            │               │ 4. 收到请求              │
 │    token = create_service_token( │   ───────▶   │    抽取 X-Service-Token: │
 │        service_name="api",     │   HTTP 请求    │    <service_jwt>         │
 │        expire_seconds=300)     │   携带 header: │                          │
 │                               │   X-Service-   │ 5. verify_service_token( │
 │ 2. 得到短期 JWT                │   Token:       │      token,             │
 │    payload={service:"api",     │   <service_jwt>│      allowed_services=  │
 │              iat, exp(+5min)}  │               │      ["api","engine"])   │
 │                               │               │                          │
 │ 3. 注入请求头                  │               │ 6. 内部调用 decode_jwt_   │
 │    X-Service-Token: ...        │               │    token() 验签+过期     │
 │    （X-User-Id / X-Tenant-Id   │               │    校验 service 声明 ∈    │
 │     透传用户上下文）            │               │    allowed_services      │
 └───────────────────────────────┘               └──────────────────────────┘
                                                          │
                                                          ▼
                                          ┌───────────────────────────────┐
                                          │ 7a. 通过 → 注入 request.state │
                                          │     service="api"，放行       │
                                          │ 7b. 失败 → 401 Unauthorized   │
                                          │     （验签失败/过期/服务不在   │
                                          │      允许列表）                │
                                          └───────────────────────────────┘
```

### 3.2 签发流程（调用方）

```
create_service_token("api", expire_seconds=300)
        │
        ▼
  读取 SECRET_KEY (env: SECRET_KEY/JWT_SECRET_KEY/JWT_SECRET)
        │  未配置 → raise RuntimeError (fail-fast，T6.2 风格)
        ▼
  读取 ALGORITHM (env: ALGORITHM/JWT_ALGORITHM，默认 HS256)
        │
        ▼
  payload = {
      "service": "api",          # 调用方身份
      "iat": now,                # 签发时间
      "exp": now + 300s          # 过期时间（默认 5 分钟）
  }
        │
        ▼
  jose_jwt.encode(payload, secret_key, algorithm) → JWT 字符串
```

### 3.3 验证流程（被调用方）

```
收到 X-Service-Token: <token>
        │
        ▼
  verify_service_token(token, allowed_services=["api","engine"])
        │
        ▼
  payload = decode_jwt_token(token)
        │  ├─ SECRET_KEY 未配置 → RuntimeError (fail-fast)
        │  ├─ 签名无效 / 已过期 → HTTPException 401
        │  └─ 成功 → 返回 payload
        ▼
  service = payload.get("service")
        │
        ▼
  service ∈ allowed_services ?
        ├─ 是 → 返回 payload（认证通过）
        └─ 否 → HTTPException 403（调用方无权访问该端点）
```

### 3.4 过期与续签处理

- **短期令牌**：默认 5 分钟过期，调用方每次发起调用前重新签发（无需持久化存储 service token）
- **无刷新令牌**：service JWT 是无状态的短期凭证，不设 refresh token，过期直接重新签发
- **密钥轮换**：所有服务读取相同 `SECRET_KEY` 环境变量，轮换时同步更新 `.env` 并重启服务即可；因 token 短期，轮换后 5 分钟内全部自然失效
- **紧急撤销**：若需立即失效，轮换 `SECRET_KEY`（所有在途 service JWT 立即验签失败）

---

## 四、与现有 create_service_token / decode_jwt_token 的关系

| 组件 | 现状 | T6.5 目标 | 改动 |
|------|------|----------|------|
| `create_service_token()` | 已实现，已成为 service JWT **签发入口** | 完成 | T6.5-P2 已接入全部调用方 |
| `decode_jwt_token()` | 已实现，6 处在用 | 作为 service JWT **验签底层** | 无需改实现 |
| `verify_service_token()` | 已新增（T6.5-P1） | service JWT 验签 + service 声明校验 | 已完成 |
| `require_service_token()` | 已新增（T6.5-P1） | FastAPI 依赖，保护内部端点 | 已完成 |
| `get_internal_call_secret()` | **已删除**（T6.5-P3） | 迁移完成后删除 | 已完成 |

**关键关系**：service JWT 方案 **完全复用** T6.2 已实现的 `create_service_token` / `decode_jwt_token`，
仅在其上叠加 `service` 声明校验层，无需新建密钥体系，无需修改现有 6 处 `decode_jwt_token` 调用方。

---

## 五、迁移路径（X-Internal-Call → Service JWT）— 全部已完成

> 遵循指令②：逐路由迁移，每路由单独 PR 便于回滚。
> 遵循指令⑤：迁移完成前不删除 shared/auth.py 与 get_internal_call_secret（T6.5-P3 已完成删除）。

### 5.1 第一阶段：补齐基础设施 — ✅ 已完成（T6.5-P1）

在 `backend/shared/auth.py` 新增两个函数（`verify_service_token` / `require_service_token`），同时清理死代码 `get_current_user` / `require_roles`，并移除弱默认值 `dev-secret-key`。

```python
def verify_service_token(token: str, allowed_services: list[str]) -> dict:
    """验证 service JWT 并校验调用方服务是否在允许列表内。"""
    payload = decode_jwt_token(token)          # 复用 T6.2 验签
    service = payload.get("service")
    if service not in allowed_services:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Service '{service}' not allowed for this endpoint",
        )
    return payload


def require_service_token(*allowed_services: str):
    """FastAPI 依赖：要求请求携带合法 service JWT 且调用方在允许列表内。

    service JWT 通过专用 ``X-Service-Token`` header 传递，不复用
    ``Authorization: Bearer``，以与用户 JWT 在中间件层物理隔离
    （委托方 M2 第三轮裁决：纵深防御 + 审计可区分 + 授权边界清晰）。
    """
    def checker(
        x_service_token: str | None = Header(default=None, alias="X-Service-Token"),
    ) -> dict:
        if not x_service_token:
            raise HTTPException(401, "Missing service token")
        return verify_service_token(x_service_token, list(allowed_services))
    return checker
```

### 5.2 第二阶段：迁移调用方至 X-Service-Token — ✅ 已完成（T6.5-P2）

17 个文件已迁移：调用方改为发 `X-Service-Token`（service JWT），被调用方改为只认 service JWT（移除 `X-Internal-Call` 回退分支）。

```python
# 调用方（已上线）
headers = {"X-Service-Token": create_service_token('api')}
```

### 5.3 第三阶段：清理 deprecated 代码 — ✅ 已完成（T6.5-P3）

33 个文件清理完成：
1. 删除 `get_internal_call_secret()` 及 `_DEPRECATED_DEFAULT_SECRET`
2. 删除所有 `X-Internal-Call` header 处理代码（兼容期回退分支）
3. 删除 `shared/auth.py` 中 `optional_auth`（死代码）
4. `INTERNAL_CALL_SECRET` 环境变量标记为 DEPRECATED，仅保留用于训练容器回调

### 5.4 第四阶段：文档同步 — ✅ 已完成（T6.5-P4）

1. 所有 README/DOCS 文件中 `X-Internal-Call` 更新为 `X-Service-Token`
2. `.env.example` 补充 `SECRET_KEY` 说明（签发 service JWT + 用户 JWT），`INTERNAL_CALL_SECRET` 标注 DEPRECATED
3. `AGENTS.md` 更新迁移状态：Phase 1-4 已完成
4. 训练容器回调路径标注 `T6.5-P3 residual, M4 migration`

### 5.5 残留项：训练容器回调（纳入 M4 迁移）

训练容器回调路径未在本轮迁移，保留旧共享密钥机制并标注 residual：

| 文件 | 残留内容 | M4 迁移方案 |
|------|---------|------------|
| `docker/training/train.py` | 发送 `X-Internal-Call-Secret` header | 改为签发 service JWT，携带 `X-Service-Token` |
| `backend/services/api/routers/admin/admin_training_utils.py` | 校验 `INTERNAL_CALL_SECRET` | 改用 `verify_service_token()` |
| `backend/services/engine/training/local_docker_orchestrator.py` | 注入 `INTERNAL_CALL_SECRET` 环境变量 | 改为注入 `SECRET_KEY` |

---

## 六、安全收益对比

| 维度 | X-Internal-Call（旧方案，已废弃） | Service JWT（当前方案） |
|------|------------------------|---------------------|
| 调用方身份 | 无（共享密钥） | 有（`service` 声明） |
| 时效 | 永久 | 5 分钟过期 |
| 密钥传输 | 每次明文传密钥 | 仅传签名令牌 |
| 端点级授权 | 无法按调用方限制 | `allowed_services` 白名单 |
| 审计 | 无法溯源 | payload 可记录调用方 |
| 轮换 | 全集群同步重启 | 改 SECRET_KEY，5 分钟自然失效 |
| 撤销 | 无法单点撤销 | 轮换密钥即全部失效 |

---

## 七、遗留决策项（需项目经理 agent2号 评审）

1. **header 命名**：✅ 已决策 · 委托方 M2 第三轮裁决 — service JWT 使用专用 `X-Service-Token` header。
   - 选择专用 `X-Service-Token`，**禁止**复用 `Authorization: Bearer`，理由：
     - **纵深防御**：service token 与用户 token 攻击面物理隔离，泄漏互不影响
     - **审计可区分**：日志/网关层一眼区分 service 调用 vs 用户调用
     - **授权边界清晰**：service token 永不授予 admin 全权
   - **中间件分工**：认证中间件分别处理 `Authorization: Bearer` → 用户 JWT；
     `X-Service-Token` → service JWT（由不同依赖/中间件解析，避免互相伪装）。
   - 该决策已在第 3.1 / 3.3 / 5.2 节同步落地。
2. **k8s_manager 环境注入**：Pod 内服务如何获取 service JWT 签发能力？建议 Pod 共享 SECRET_KEY 即可自行签发。
3. **`optional_auth` 死代码**：本轮未删（不在指令明确范围），建议第四阶段一并清理。
4. **`create_service_token` 当前未被调用**：作为过渡件已就绪，待第二阶段开始接入。

---

## 八、迁移落地范围汇总（T6.5-P1 ~ P4 全部完成）

### 8.1 T6.5-P1（基础设施 + 死代码清理）
- 删除死代码 `get_current_user` / `require_roles`（grep 确认零路由依赖）
- `AuthManager.create_access_token` / `verify_token` 增加 `_ensure_secret_configured()` fail-fast
- `config/settings.py` 移除弱默认值 `dev-secret-key`
- 新增 `verify_service_token()` / `require_service_token()`（service JWT 验签 + service 声明校验）

### 8.2 T6.5-P2（调用方迁移）
- 17 个文件迁移至 `X-Service-Token`（service JWT），调用方改为 `create_service_token()` 签发
- 被调用方改为 `verify_service_token()` / `require_service_token()` 校验

### 8.3 T6.5-P3（deprecated 代码清理）
- 删除 `get_internal_call_secret()` / `_DEPRECATED_DEFAULT_SECRET` / `optional_auth`（死代码）
- 删除全部 `X-Internal-Call` header 处理代码（33 个文件清理）
- `INTERNAL_CALL_SECRET` 环境变量标记 DEPRECATED，仅保留训练容器回调用途

### 8.4 T6.5-P4（文档同步）
- 所有 README/DOCS 文件 `X-Internal-Call` 更新为 `X-Service-Token`
- `.env.example` 补充 `SECRET_KEY` 说明，`INTERNAL_CALL_SECRET` 标注 DEPRECATED
- `AGENTS.md` 迁移状态更新为 Phase 1-4 已完成
- 训练容器回调路径标注 `T6.5-P3 residual, M4 migration`

### 8.5 残留项（纳入 M4 迁移）
- 训练容器回调路径（`docker/training/train.py` → `admin_training_utils.py` → `local_docker_orchestrator.py`）保留旧共享密钥机制
- 标注 `T6.5-P3 residual, M4 migration`，便于 M4 识别与迁移
