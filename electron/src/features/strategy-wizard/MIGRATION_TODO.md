# Strategy Wizard V2 迁移清单

## P0（先做）

1. [x] 定义统一领域模型：`WorkingPool / SavedPoolVersion / ActivePoolVersion`。  
2. [x] 在 `services/` 完成接口适配层，禁止组件直接请求 API。  
3. [x] 在 `store/` 完成单一真相源状态与状态机约束。  

## P1（核心流程）

1. [x] 迁移 Step1：自然语言、CSV、全市场、云端复用统一写入 `workingPool`。  
2. [x] 迁移 Step2：只负责确认、保存版本、激活版本。  
3. [x] 对齐“下一步门禁”：未保存激活时禁止进入 Step3。  

## P2（性能韧性）

1. [x] 大池加载走轻量接口 + 批处理 + 两阶段状态提交（先代码后特征）。  
2. [x] 关键读取接口增加一次重试与同步状态回退。  
3. [~] 错误提示去重与分级（5秒窗口去重）。 (待进一步优化)

## P3（切流与清理）

1. [x] 增加入口开关，支持灰度切换到 V2（`VITE_USE_STRATEGY_WIZARD_V2=1`）。  
2. [x] 核心 UI 组件全部迁移至 V2 目录并完成逻辑解耦。
3. [ ] 验证通过后主路由切换至 V2。  
4. [ ] 删除旧目录冗余代码并更新文档。  
